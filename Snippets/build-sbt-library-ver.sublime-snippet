<snippet>
<content>
<![CDATA[
// single dependency, % "configuration" is optional
libraryDependencies += "${1:groupID}" % "${2:artifactID}_${3:scalaVersion}" % "${4:revision}"

]]>
</content>
	<tabTrigger>sbt</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.scala</scope>
	<description>build.sbt libDep %% eq</description>
</snippet>
